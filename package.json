{
  "name": "tap-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "wrangler dev --port 8787 --test-scheduled",
    "build": "vite build",
    "preview": "wrangler dev",
    "check": "wrangler types worker/worker-configuration.d.ts > /dev/null && tsc --noEmit && tsc --noEmit -p worker",
    "check:api": "node scripts/check-api.mjs",
    "db:migrate:local": "wrangler d1 migrations apply tap-watch --local",
    "db:migrate:remote": "wrangler d1 migrations apply tap-watch --remote",
    "db:seed:local": "node seed/build-seed-sql.mjs e17 --sample-listings && wrangler d1 execute tap-watch --local --file seed/out/seed-e17.sql",
    "db:seed:remote": "node seed/build-seed-sql.mjs e17 --sample-listings && wrangler d1 execute tap-watch --remote --file seed/out/seed-e17.sql",
    "seed:pubs": "node seed/fetch-pubs.mjs",
    "seed:map": "node seed/open-map.mjs",
    "map:build": "node seed/build-basemap.mjs"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "wrangler": "4.133.0"
  },
  "dependencies": {
    "maplibre-gl": "6.10.0"
  }
}
